## Review — 2026-09-22 — Verdict: NEEDS REVISION
Scope signal: M
Specialists: game-designer, systems-designer, ux-designer, audio-director, gameplay-programmer, godot-specialist, qa-lead, creative-director (senior)
Blocking items: 11 | Recommended: 8
Summary: The design is right; the specification has holes — creative-director ruled NEEDS REVISION, not MAJOR, with all fixes landing in one session and no re-architecture. The two findings that matter most are both "a verified claim that isn't": (1) `ring_distance` assumes Euclidean mod, but GDScript `%` is sign-of-dividend — at from=2,to=0,n=3 it yields -2 and press_cost=0, making the BLOCKING "every press_cost <= 3" AC provably true on paper and silently false in code (fix: `posmod()`, and enumerate all 9 pairs in the AC); (2) `DisplayServer.get_display_safe_area()` is cited as settled fact in Core Rule 13, UI Requirements and two ACs while the project's own `docs/engine-reference/godot/modules/mobile-export.md:43` lists it as OPEN/unverified for 4.7 — it was never doctool-checked, unlike `offset_transform_*` and `vibrate_handheld`. Compounded by a real coordinate-space gap: that API returns physical screen pixels while the project's 4.7 stretch mode is `canvas_items`/`expand`, and neither `lcd_scale` nor Core Rule 13 converts between them. Remaining blockers: guard `lcd_scale` against division by zero before the clamp (LCD_W/H owned by the undesigned LCD Renderer); add MENU directional glyphs + AC (the ring ships at MVP ~wk3 but Hatching Onboarding is Vertical Slice ~wk5, so the MVP hypothesis is tested with no teacher); resolve "lights" in this GDD (formula, affordance, AC) or hand off to Care Actions with a dated owner; define `request_confirm()` queue semantics (single slot, latest-wins, dropped on timeout, re-entrancy) and add the queued flag as a transition-table trigger; split the mute AC per Ruling B; add the ACTING tick rule per Ruling C; name the safe-area change detection mechanism plus a stuck-press deferral timeout; enforce n==3 structurally; specify the "muted tick" audio (Hz/ms/waveform).
Prior verdict resolved: First review

### Senior rulings (creative-director)
- **A — The ring stays.** game-designer and ux-designer both called cycle-then-select blocking (fantasy-corrosion; unteachability). Overruled: it is load-bearing for the <=3-press constraint, prototype-validated for feel, and Pillar 5 bars the alternatives. It is missing scaffolding, not wrong — add persistent directional glyphs beside A and C rendered on the LCD in MENU, plus an AC. Revisit with playtest data, not prediction, if testers still cycle blindly. The CD also rejected game-designer's framing of `press_cost` as a player-facing efficiency economy: it is an author-facing verification metric the player never sees.
- **B — Split the mute AC (qa-lead upheld).** "A gate no test can pass is not a strong pillar, it is an unfalsifiable one, and unfalsifiable gates get quietly waived." Replace with (i) BLOCKING, automatable in gdUnit4: with sfx and haptics disabled, every state exposes a distinct visual state, cursor index and action-outcome trigger via the structural API; (ii) ADVISORY, human: a full mute-playthrough screenshot set with lead sign-off, named as a required Vertical Slice deliverable in `production/qa/evidence/`.
- **C — The tick fires during ACTING.** Rule 11 discards the *action*, never the *acknowledgement*. Governing principle to write into Core Rule 2: **"Acknowledgement is unconditional; action is conditional."** Resolves the Rule 2 / Rule 11 contradiction without weakening either.
- **CONFIRM is kept** (game-designer's "modal dialog wearing toy clothes" rejected — a two-button yes/no on a three-button toy is a toy, and the concept already says graduation waits for a player press). Add only a 150ms input cooldown on CONFIRM entry so reflexive C-mashing cannot auto-cancel.

### Explicitly downgraded for a solo 3-week MVP
Record as Open Questions, do not block: audio tone distinguishability, repeat-press fatigue, ducking/overlap policy (all playtest-tuning, not design defects); the 48dp narrow-safe-area floor and silent-discard motor accessibility (real, but Alpha-tier); n=1/n=2 rejection, negative `avail`, direction-of-travel; Tween kill policy and the 4.7 antialiasing-feather change (implementation notes for the programmer, not GDD content).

### Disposition
User elected to stop after review and revise in a separate session (2026-09-22). The 11 blocking items remain open. Systems index marked "In Review".
